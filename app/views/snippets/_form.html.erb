<%= form_for(@snippet) do |f| %>
  <% if @snippet.errors.any? %>
    <div class="alert alert-danger">
      <h4><%= pluralize(@snippet.errors.count, "error") %> prohibited this snippet from being saved:</h4>

      <ul>
      <% @snippet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, :class => "form-control" %>
  </div>
  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code, :class => "form-control" %>
  </div>
  <div class="field">
    <%= f.label "Explanation" %><br>
    <%= f.text_area :expl_md, :class => "form-control" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn btn-primary" %>
  </div>
<% end %>
